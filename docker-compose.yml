services:
  postgres:
    image: postgres:15-alpine
    container_name: fligoo-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-fligoo}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-fligoo}
      POSTGRES_DB: ${POSTGRES_DB:-testfligoo}
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./db/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-fligoo} -d ${POSTGRES_DB:-testfligoo}"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
